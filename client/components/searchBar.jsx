import React from 'react';

const searchBar = (props) => {

    return (
      <input 
        id='searchBar'
        type='text'
        name='searchBar'
        placeholder='...search'
      />
    );
};

export default searchBar;